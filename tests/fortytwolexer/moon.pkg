import {
  "moonbitlang/ulex-runtime/lexbuf",
  "moonbitlang/core/int",
}

formatter(ignore: [ "fortytwolexer.mbt" ])

rule(name: "moonlex", command: "moon runwasm $mod_dir/.. $input -o $output")

dev_build(
  rule: "moonlex",
  input: "fortytwolexer.mbtx",
  output: "fortytwolexer.mbt",
)
