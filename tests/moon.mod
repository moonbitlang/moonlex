name = "moonbitlang/ulex_tests"

version = "0.3.28"

import {
  "moonbitlang/ulex-runtime@0.4.3",
}

readme = "README.md"

repository = "https://github.com/moonbitlang/moonlex.git"

license = "Apache-2.0"

keywords = [ ]

description = ""

options(
  exclude: [ "tests" ],
)
